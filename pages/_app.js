import "@/styles/globals.css";import { SessionProvider } from "next-auth/react";import { useEffect,useState } from "react";import { t as tr } from "@/lib/i18n";export default function MyApp({Component,pageProps:{session,...pageProps}}){const [lang,setLang]=useState("bn");useEffect(()=>{setLang(localStorage.getItem("lang")||"bn")},[]);const t=(k)=>tr(lang,k);return(<SessionProvider session={session}><Component {...pageProps} t={t} lang={lang} /></SessionProvider>)}
