import Header from "@/components/Header";import { useState } from "react";export default function Signup({t}){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");async function onSubmit(e){e.preventDefault();const res=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,email,password})});const data=await res.json();if(res.ok){alert("Signup success, now login");location.href="/login";} else alert(data.message||"Failed");}return(<div><Header t={t}/><main className="max-w-md mx-auto p-4"><h2 className="text-2xl font-bold mb-4">{t("signup")}</h2><form onSubmit={onSubmit} className="flex flex-col gap-3"><input className="input" placeholder={t("name")} value={name} onChange={e=>setName(e.target.value)} /><input className="input" placeholder={t("email")} value={email} onChange={e=>setEmail(e.target.value)} /><input className="input" type="password" placeholder={t("password")} value={password} onChange={e=>setPassword(e.target.value)} /><button className="btn bg-green-600 hover:bg-green-500">{t("signup")}</button></form></main></div>)}
